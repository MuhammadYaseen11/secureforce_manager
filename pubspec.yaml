name: secureforce_manager
description: "SecureForce Manager, a mobile app that will be designed to efficiently manage their staff, availability, and shifts assignments."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.1
  
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  hive_generator: ^2.0.1
  build_runner: ^2.3.3

flutter:
  uses-material-design: true
  # assets:
  #   - assets\data\dummy_staff.json # Asset loading is currently disabled as it is not required for this project.
